{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker-exec",
            "type": "shell",
            "command": "docker exec -ti debugpy python /app/${relativeFile} ",
            "isBackground": true
        }
    ]
}